// Re-export all enums from Prisma (single source of truth)
export {
  UserRole,
  OrganizationStatus,
  StudentStatus,
  LeadStatus,
  AttendanceStatus,
  CourseStatus,
  PaymentMethod,
  PaymentStatus,
  ExpenseCategory,
} from '@prisma/client';
