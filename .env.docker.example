# Copy to .env when running `docker compose up`
POSTGRES_PASSWORD=crmpass
JWT_ACCESS_SECRET=replace-with-64-byte-hex-secret
JWT_REFRESH_SECRET=replace-with-different-64-byte-hex-secret
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
